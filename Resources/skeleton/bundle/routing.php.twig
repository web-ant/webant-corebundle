<?php

{% block use_statements %}
use Symfony\Component\Routing\RouteCollection;
use Symfony\Component\Routing\Route;
{% endblock use_statements %}

{% block definition %}
$collection = new RouteCollection();
{% endblock definition %}

{% block body %}
$collection->add('{{ extension_alias }}_{{ extension_alias }}', new Route('/hello/{name}', array(
    '_controller' => '{{ namespace }}\Controller\{{ bundle_basename }}Controller',
)));
{% endblock body %}

{% block return %}
return $collection;
{% endblock return %}
